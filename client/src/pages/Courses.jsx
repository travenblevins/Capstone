import Navbar from "../components/Navbar";

const Courses = () => {
  return (
    <div>
      <Navbar />
      <div>
        This will be where you can see all the Courses, search and add a course
      </div>
    </div>
  );
};

export default Courses;
